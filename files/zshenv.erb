if [[ -z "$PATH" || "$PATH" == "/bin:/usr/bin" ]]
then
  export PATH="/usr/local/bin:/usr/bin:/bin:/usr/games"
fi

export JAVA_HOME=/usr/lib/<%= @jdk_dir %>
export CLASSPATH=.:$JAVA_HOME/lib
export PATH=$PATH:$HOME/bin:$JAVA_HOME/bin
